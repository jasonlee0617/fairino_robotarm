# 半自动手眼标定助手参数；分组仅用于可读性，launch 会展开为 ROS 扁平参数。
manual_calibration_assistant:
  ros__parameters:
    # runtime：运行环境
    runtime:
      use_sim_time: false                         # Gazebo=true，实机=false

    # frames_topics：坐标系、相机输入和 ArUco 定义
    frames_topics:
      calibration_type: eye_in_hand              # eye_in_hand 或 eye_on_base
      base_frame: base_link                      # 机器人基坐标系
      ee_frame: tool0                            # Fairino 末端工具坐标系
      tracking_base_frame: camera_color_optical_frame
      tracking_marker_frame: calibration_aruco
      marker_id: 1
      image_topic: /camera/camera/color/image_raw
      camera_info_topic: /camera/camera/color/camera_info
      aruco_dictionary_id: DICT_5X5_250
      camera_intrinsics_source: p

    # motion：MoveIt 规划与动作时序
    motion:
      move_group_name: robot_arm
      move_group_ns_fairino: /move_group_fairino
      planning_pipeline_id: fairino
      planner_id: birrt*
      joint_names: [j1, j2, j3, j4, j5, j6]
      max_velocity: 0.2
      max_acceleration: 0.2
      allowed_planning_time: 30.0               # 秒
      max_step_size: 0.05                       # 米
      position_tolerance: 0.005                 # 米
      orientation_tolerance: 0.005              # 弧度
      allowed_start_tolerance: 0.1
      action_delay: 0.2                         # 秒
      settle_time_sec: 1.0                      # 秒
      keyboard_poll_period: 0.1                 # 秒
      start_wait_poll_period: 0.1               # 秒
      moveit_ready_timeout: 30.0                # 秒
      moveit_ready_poll_interval: 0.2           # 秒

    # vision_quality：标记尺度、距离和稳定帧质量门控
    vision_quality:
      marker_size_m: 0.070
      marker_distance_min_m: 0.20
      marker_distance_max_m: 1.00
      stable_frames: 10
      stable_marker_timeout_sec: 5.0
      minimum_corner_margin_px: 60.0
      minimum_marker_side_px: 90.0
      maximum_center_std_px: 4.0
      maximum_marker_depth_std_m: 0.003
      maximum_marker_angle_std_deg: 0.8

    # sampling_gates：关节静止与样本多样性要求
    sampling_gates:
      joint_stationary_max_position_delta_rad: 0.0001
      joint_stationary_window_sec: 0.30
      joint_stationary_timeout_sec: 5.0
      stable_tf_settle_sec: 0.15
      minimum_samples: 15
      minimum_solution_samples: 14
      minimum_translation_delta_m: 0.006
      minimum_rotation_delta_deg: 3.0
      minimum_translation_span_m: 0.04
      minimum_rotation_span_deg: 20.0

    # solver_validation：求解器一致性、固定标定板和仿真真值门限
    solver_validation:
      algorithm_names: [OpenCV/Park, OpenCV/Horaud]
      maximum_algorithm_translation_delta_m: 0.003
      maximum_algorithm_rotation_delta_deg: 1.0
      maximum_camera_translation_norm_m: 0.30
      maximum_eye_on_base_camera_translation_norm_m: 3.0
      maximum_marker_position_rms_m: 0.002
      maximum_marker_rotation_rms_deg: 0.70
      fixed_marker_refinement_translation_sigma_m: 0.0005
      fixed_marker_refinement_rotation_sigma_deg: 0.30
      fixed_marker_refinement_max_iterations: 25
      ground_truth_check_enabled: false
      ground_truth_max_translation_error_m: 0.003
      ground_truth_max_axis_error_m: 0.002
      ground_truth_max_rotation_error_deg: 1.0

    # output：标定结果保存位置和命名
    output:
      calibration_output_directory: $HOME/fairino_robotarm/src/calibration_ws/hand_eye_calibration/calib/sim
      calibration_file_prefix: robot_calibration
